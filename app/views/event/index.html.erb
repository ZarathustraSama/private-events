<h1>Here are all the events available!</h1>
<% if @events %>
  <ul>
    <% @events.each do |event| %>
      <li>
        <div class="Event">
          <p>event.name</p>
        </div>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>...or not I guess?</p>
<% end %>